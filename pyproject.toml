[project]
name = "zammad-ticket-archiver"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
[tool.pytest.ini_options]
addopts = "-q"
# forced-separate-dev-and-release-2:maintenance
